server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # GGM-URI's (/<domein>/<klasse>) resolven naar de verkenner, die het pad
    # opzoekt in de triple store (linked-data dereferencing).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
